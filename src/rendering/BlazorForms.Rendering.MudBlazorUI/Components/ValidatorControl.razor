@using MudBlazor
@using BlazorForms.FlowRules

<div>
    @foreach (var validation in Validations)
    {
        @if (validation.ValidationResult == RuleValidationResult.Warning)
        {
            <MudText Typo="Typo.caption" style="color: DarkOrange;">@($"{validation.RuleCode}: {validation.ValidationMessage}")</MudText>
        }
        else if (validation.ValidationResult == RuleValidationResult.Error)
        {
            <MudText Typo="Typo.caption" style="color: red;">@($"{validation.RuleCode}: {validation.ValidationMessage}")</MudText>
        }
    }
</div>

@code {
    [Parameter]
    public IEnumerable<RuleExecutionResult> Validations { get; set; }

    protected override async Task OnInitializedAsync()
    {
    }
}
