@page "/demoForm/{store:bool}"

<PageTitle>Form</PageTitle>

<DynamicForm FlowName="@typeof(BlazorFormsDemoFlows.FluentArtelProjectSettingsFlow).FullName" Pk="0" AllowAnonymousAccess="true" AllowFlowStorage="@Store" NavigationSuccess="/"/>

@code {
    [Parameter]
    public bool Store { get; set; }
}
