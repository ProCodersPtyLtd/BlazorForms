@page "/flow-board"
@using BlazorForms.Rendering.Interfaces
@using BlazorForms.Flows.Definitions
@using BlazorForms.Rendering.State
@using BlazorForms.Rendering.Model
@using BlazorForms.Rendering.MudBlazorUI.Components
@using MudBlazor

<FlowBoard TFlow=MudBlazorUIDemo.Flows.SampleStateFlow Items=@_items />

@code {
    List<FlowBoardCard> _items = (new FlowBoardCard[]
    {
        new FlowBoardCard { State = "Leads", Title = "IBM", Details = "Severe problems with obvious code quality", Order = 0 },
        new FlowBoardCard { State = "Leads", Title = "Telstra", Details = "Too ineffective IT department, unsecured and overprised services", Order = 1 },
        new FlowBoardCard { State = "Leads", Title = "Bounty", Order = 2 },
        new FlowBoardCard { State = "Contacted", Title = "Hyundai", Order = 0 },
        new FlowBoardCard { State = "Won", Title = "Igloo Limited", Order = 0 },
    })
    .ToList();

    private ListFormOptions Options = new ListFormOptions
        {
            Variant = Variant.Outlined,
            MudBlazorProvidersDefined = true
        };
}
